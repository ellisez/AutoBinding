name: model_binding
description: ModelBinding is a lightweight MVVM bidirectional binding state management framework for data sharing and synchronization.
version: 2.0.0
homepage: 'https://github.com/ellisez/ModelBinding'
topics:
  - mvvm
  - json
  - codegen

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
