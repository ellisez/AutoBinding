name: model_binding
description: A new Flutter package project.
version: 1.0.0
homepage: 'https://github.com/ellisez/ModelBinding'

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  map_model: ^1.0.5
